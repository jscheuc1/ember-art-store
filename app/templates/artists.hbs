<div class="container">
<div class="row">
  <div class="col-md-4">
    <div class="panel panel-default">
    <div class="panel-heading">Artists</div>
      {{#each model as |artist|}}
      <div class="panel-body">
        {{link-to artist.name "artists.artist" artist}}
      </div>
      {{/each}}

    </div>
  </div>
</div>
{{artist-show artist=model}}
</div>
